import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cicero2.app',
  appName: 'cicero2',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
